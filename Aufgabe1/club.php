<?php 

	if($_POST['formSubmit'] == "Submit") 
	{
        $varClub = $_POST['club'];
        if ($varClub === 'other') {
          // show input field to select other club 
        }
        
        
    
 
 	}

?>
